let value = 0
basic.forever(function () {
    value = pins.map(
    pins.analogReadPin(AnalogPin.P1),
    5,
    800,
    1,
    Math.round(24)
    )
    for (let index = 0; index < value; index++) {
    	
    }
})
basic.forever(function () {
    value = pins.map(
    pins.analogReadPin(AnalogPin.P1),
    5,
    800,
    1,
    Math.round(24)
    )
    for (let index = 0; index < value; index++) {
    	
    }
})
